# Secrets — never commit these
.env
.env.local
.env.*.local

# Machine-local paths (copy from paths.local.json.example)
config/paths.local.json

# Python cache
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store

# Node (fal.ai Canva background generator)
node_modules/
