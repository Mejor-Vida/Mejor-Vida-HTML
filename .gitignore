__pycache__/
*.py[cod]

.DS_Store
.env
.env.local
node_modules/
.env.*.local
.vercel
.env*.local
agent/screenshots/
agent/logs/

# Playwright / local browser session dirs (if added by automation)
integrations/.playwright_ic_session/

# Generated local Facebook preview (open in browser before publishing)
FB/post-preview.html
FB/post-preview-*.html

# Local-only licensing / PII for agent workflows (never commit)
docs/*-private.local.md

# Kontext multi-candidate runs (large; keep finals in mvi-chat-avatar/*.png only)
img/mvi-chat-avatar/_candidates/

# Unredacted license PDFs (personal address) — do not publish
licenses/private-originals/
.venv-lic-redact/
